# Production build (npm run build): the React app is served BY FastAPI from the
# same origin, so API calls just use the relative path /api - no host needed.
# This file has no secrets and IS committed to git so the Docker build uses it.
VITE_API_URL=/api
